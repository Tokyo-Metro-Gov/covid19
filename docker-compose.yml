version: '3'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - /var/www/node_modules
      - ./:/var/www
    ports:
      - "3000:3000"
    tty: true
    restart: always
